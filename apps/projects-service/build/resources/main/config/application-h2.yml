spring:
  datasource:
    url: jdbc:h2:mem:event_planner;MODE=PostgreSQL
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
