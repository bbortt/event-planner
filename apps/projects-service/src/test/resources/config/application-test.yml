jwt:
  audience: http://localhost:8081
logging:
  level:
    root: warn
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event_planner_test?currentSchema=projects_service
    username: projects_service
    password: projects_service
  jpa:
    hibernate:
      ddl-auto: create-drop
  security:
    csrf:
      enabled: false
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${wiremock.server.base-url}
wiremock:
  server:
    base-url: http://localhost:${wiremock.server.port}/
