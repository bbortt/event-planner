type Member {
  id: ID!
  project: Project!
  accepted: Boolean!
  auth0User: Auth0User!
  permissions: [Permission!]!
}

extend type Query {
  listMembers(projectId: ID!): [Member!]!
  getMember(memberId: ID!): Member!
}

extend type Mutation {
  joinProject(token: String): Member!
}
