table:
  name: permission
  schema: public
array_relationships:
  - name: members
    using:
      foreign_key_constraint_on:
        column: permission_id
        table:
          name: member_permission
          schema: public
select_permissions:
  - permission:
      columns:
        - id
      filter: {}
    role: user
