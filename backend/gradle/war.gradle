apply plugin: "war"

bootWar {
    mainClassName = "io.github.bbortt.event.planner.BackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
